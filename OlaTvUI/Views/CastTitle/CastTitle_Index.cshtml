@using EntityLayer.Concrete
@model List<CastTitle>
@{
    ViewData["Title"] = "CastTitle_Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    var i = 1;
}

<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>#</th>
            <th>ID</th>
            <th>Cast Name Surname</th>
            <th>Title Name</th>
            <th>Status</th>
            <th>Change Status</th>
            <th>CRUD Operations</th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th>@i</th>
                <td>@item.CastTitleId</td>
                <td>@item.Cast.CastNameSurname</td>
                <td>@item.Title.TitleName</td>
                <td>@item.IsDelete</td>
                <td>
                    <a href="/CastTitle/CastTitle_Activate/@item.CastTitleId" class="btn btn-outline-success text-center tik" style="margin-right:10px;">Activate <i class="fa fa-check" style="color:black"></i></a>
                    <a href="/CastTitle/CastTitle_Deactivate/@item.CastTitleId" class="btn btn-outline-secondary text-center carpi" style="margin-right:10px;">Deactivate <i class="fa fa-times"style="color:black" aria-hidden="true"></i></a>
                </td>
                <td>
                    <a href="/CastTitle/CastTitle_Update/@item.CastTitleId" type="button" class="btn btn-outline-primary" style="margin-right:10px;">Update</a>
                    <button type="button" class="btn btn-outline-danger text-center" data-bs-whatever="@item.CastTitleId" data-bs-toggle="modal" data-bs-target="#decisionPopup">Delete</button>
                </td>
            </tr>
            i++;
        }
    </tbody>
</table>

<div class="text-center d-flex justify-content-start p-2">
    <a href="/CastTitle/CastTitle_Add" class="btn btn-success d-flex justify-content-center">Add CastTitle<ion-icon name="add" style="font-size:25px; margin-left:10px;"></ion-icon></a>
</div>

<!-- Modal -->
@*#decisionPopup Model*@

<div class="modal fade" id="decisionPopup" tabindex="=-1" aria-labelledby="deleteLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h1 class="modal-title" id="deleteLabel">Warning</h1>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Are you sure you want to delete?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">No</button>
                <a id="delete-btn" href="" class="btn btn-success">Yes</a>
            </div>
        </div>
    </div>
</div>


<script>
    $(document).ready(function () {
        $('#decisionPopup').on('show.bs.modal', function (event) {
            var button = $(event.relatedTarget);
            var CastTitleId = button.attr('data-bs-whatever');
            var a = document.querySelector("#delete-btn");
            a.href = "/CastTitle/CastTitle_Delete/" + CastTitleId;
        });
    });
</script>

